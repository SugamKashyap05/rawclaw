# Docker context env vars — apps use these when running against containerized deps
# Load with: export $(cat .env.docker | grep -v '^#' | xargs)
REDIS_URL=redis://localhost:6379
CHROMA_HOST=localhost
CHROMA_PORT=8010
