global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: rawclaw-worker
    static_configs:
      - targets: ["swarm-worker:9090"]
  - job_name: rawclaw-agent
    static_configs:
      - targets: ["host.docker.internal:9091"]
